name: SimpeCICD
on: 
 push:   
  branches:
    - master
jobs:
   GetVersions:
    runs-on: ubuntu-latest
    steps:
       - name: netversion
         run: dotnet --version

   RunsCommands:
    runs-on: ubuntu-latest
    steps:
      - name: runs-some-linux-command 
        run: |
           NAME=secret.MYNAME
           echo "My name is ${NAME}"
           echo "I am  $NAME"
       
   
  
      

    
  
